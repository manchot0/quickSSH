# default user
user = 'changeme'

# dictionary of additional user, {'unique id' : 'user'}
# exemple : other_user = {'abcdefg' : 'seconduser', 'ajdhtpdb' : 'thirduser'}
other_user = {}

# default port
port = '22'

# absolute path to the host file
host_file = '/path/to/qsh/hosts.yml'

# encrypt_key define the key used to encrypt the file and to read from it.
# if you use a plain file just comment the variable.
#you can generate it with qsh generate_key
#encrypt_key = ''
